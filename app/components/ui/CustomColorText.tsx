import React from 'react'

const CustomColorText = ({ children }: { children: React.ReactNode }) => {
  return (
    <span className='text-[#B590E7]'>
      {children}
    </span>
  )
}

export default CustomColorText
